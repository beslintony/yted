# golangci-lint configuration for YTed (v1 format)

run:
  go: "1.24"
  timeout: 5m

linters:
  enable:
    - errcheck      # Check for unchecked errors
    - gosimple      # Simplify code
    - govet         # Vet examines Go source code
    - ineffassign   # Detect ineffectual assignments
    - staticcheck   # Go static analysis
    - unused        # Check for unused constants, variables, functions
    - gocritic      # Opinionated go linter
    - misspell      # Find commonly misspelled words
    - revive        # Drop-in replacement for golint
    - bodyclose     # Check for unclosed http bodies
    - noctx         # Find functions without context
    - prealloc      # Find slice declarations that could be preallocated
    - unconvert     # Remove unnecessary type conversions
    - unparam       # Report unused function parameters

linters-settings:
  revive:
    rules:
      - name: exported
        arguments:
          - checkPrivateReceivers
          - sayRepetitiveInsteadOfStutters
      - name: context-as-argument
      - name: context-keys-type
      - name: error-return
      - name: error-strings
      - name: var-naming
      - name: indent-error-flow
      - name: range
      - name: receiver-naming
      - name: time-naming
      - name: unexported-return
      - name: error-naming
      - name: empty-block
      - name: superfluous-else
      - name: unused-parameter
      - name: unreachable-code
  gofmt:
    simplify: true
  goimports:
    local-prefixes: yted
  gci:
    sections:
      - standard
      - default
      - prefix(yted)

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-use-default: false
  exclude:
    # Exclude shadow checking for err variable (common pattern)
    - 'shadow: declaration of "err" shadows'
    # Exclude know deprecation warnings
    - "SA1019"
    # Exclude stuttering warnings for log package types
    - 'type name will be used as log.LogEntry'
    - 'type name will be used as log.LogLevel'
  exclude-rules:
    # Exclude some linters from running on tests
    - path: _test.go
      linters:
        - gocyclo
        - errcheck
        - dupl
        - gosec
        - lll
    # Exclude SA5011 false positives in tests (nil pointer after check pattern)
    - path: _test.go
      linters:
        - staticcheck
      text: "SA5011"

output:
  formats:
    - format: colored-line-number

